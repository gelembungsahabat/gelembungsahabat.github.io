# newpersonalblog

## How to run local?
you can install http-server from npm: <br> 
```npm i http-server -g```

after installed, just cd your terminal on this project, and run: <br>
```http-server```

<br>
<br>

## Stack
- trying to be `html` + `css` only
- will be using `htmx` stuff
- [eva-icons](https://github.com/akveo/eva-icons) iconpack