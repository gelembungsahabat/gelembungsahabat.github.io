{
  "name": "gelembungsahabat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=master --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.14.5"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^2.1.0",
    "push-dir": "^0.4.1"
  }
}
